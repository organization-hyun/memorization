package com.memorization.domain.examHistory.item;

import org.springframework.data.jpa.repository.JpaRepository;

public interface ExamHistoryItemRepository extends JpaRepository<ExamHistoryItem, Long> {
}
